import React from 'react';
import PropTypes from 'prop-types';
import './First.css';

const First = () => (
  <div className="First">
    First Component
  </div>
);

First.propTypes = {};

First.defaultProps = {};

export default First;
